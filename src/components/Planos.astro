<section id="planos" class="plans-section">
  <div class="plans-container">
    <header class="plans-header">
      <span class="section-tag">Preço Fixo e Transparente</span>
      <h2 class="category-title">Sua Bio Profissional sem Complicação</h2>
      <p class="plans-subtitle">
        Sem assinaturas mensais ou taxas escondidas. Um valor acessível para colocar sua marca em outro nível.
      </p>
    </header>

    <div class="single-plan-wrapper">
      <div class="plan-card single-card">
        <div class="plan-badge-top">Oferta Especial</div>
        
        <div class="plan-header">
          <h3 class="plan-title">Mini Site para Bio</h3>
          <p class="plan-ideal">
            Ideal para: Advogados, Médicos, Psicólogos, Nutricionistas, Consultores, Criadores de Conteúdo e Serviços em Geral.
          </p>
          
          <div class="plan-price">
            <div class="price-main">
              <span class="price-currency">R$</span>
              <span class="price-value">39</span>
              <span class="price-cents">,99</span>
            </div>
            <div class="price-details">
              
              <span class="price-renewal">Renovação anual </span>
            </div>
          </div>
        </div>

        <ul class="plan-features">
          <li><strong>1 ano online incluso</strong> (sem mensalidades)</li>
          <li><strong>Endereço personalizado incluso</strong> <code>seusite.pages.dev</code></li>
          <li><strong>Personalização Completa:</strong> Sua foto/logo, cores da sua marca e bio ajustada</li>
          <li><strong>Botões de Ação Direta:</strong> WhatsApp, Redes Sociais, Telefone e E-mail</li>
          <li><strong>Localização Interativa:</strong> Botão com Google Maps</li>
          <li><strong>Design Ultra Rápido & Responsivo:</strong> Perfeito no celular e computador</li>
          <li><strong>Entrega expressa:</strong> Seu link no ar em 24h a 48h</li>
        </ul>

        <div class="plan-footer">
          <a
            href="https://wa.me/5541984940848?text=Ol%C3%A1!%20Quero%20garantir%20meu%20Mini%20Site%20para%20Bio%20por%20R$%2039,99."
            target="_blank"
            rel="noopener noreferrer"
            class="btn-plan btn-featured"
          >
            Garantir Meu Mini Site Agora
          </a>
          <span class="guarantee-text">🔒 Compra segura • Atendimento humanizado no WhatsApp</span>
        </div>
      </div>
    </div>

  </div>
</section>

<style>
@import url('https://fonts.googleapis.com/css2?family=Sora:wght@600;700;800&family=Inter:wght@400;500;600;700&display=swap');

.plans-section {
  padding: 80px 20px;
  background-color: var(--bg-soft, #110c16);
  border-top: 1px solid var(--border-soft, rgba(168, 85, 247, 0.15));
  font-family: 'Inter', sans-serif;
}

.plans-container {
  max-width: 900px;
  margin: 0 auto;
}

.plans-header {
  text-align: center;
  margin-bottom: 48px;
}

.section-tag {
  display: inline-block;
  font-family: 'Sora', sans-serif;
  font-size: 0.78rem;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  color: var(--highlight, #a855f7);
  background: rgba(168, 85, 247, 0.12);
  border: 1px solid rgba(168, 85, 247, 0.25);
  padding: 5px 14px;
  border-radius: 20px;
  margin-bottom: 12px;
}

.category-title {
  font-family: 'Sora', sans-serif;
  font-size: clamp(1.8rem, 3vw, 2.4rem);
  font-weight: 800;
  color: #ffffff;
}

.plans-subtitle {
  color: var(--text-muted, #c4b8d7);
  font-size: 1.02rem;
  margin-top: 10px;
}

/* Wrapper do Card Único */
.single-plan-wrapper {
  display: flex;
  justify-content: center;
  margin-bottom: 48px;
}

.plan-card.single-card {
  width: 100%;
  max-width: 620px;
  background: linear-gradient(180deg, rgba(168, 85, 247, 0.12) 0%, var(--bg-elevated, #1a1422) 30%);
  border: 2px solid var(--highlight, #a855f7);
  border-radius: 24px;
  padding: 40px 32px;
  position: relative;
  box-shadow: 0 20px 50px rgba(168, 85, 247, 0.2);
}

.plan-badge-top {
  position: absolute;
  top: -14px;
  left: 50%;
  transform: translateX(-50%);
  background: linear-gradient(135deg, var(--primary, #8343c3), var(--highlight, #a855f7));
  color: #ffffff;
  font-family: 'Sora', sans-serif;
  font-size: 0.75rem;
  font-weight: 700;
  padding: 5px 18px;
  border-radius: 20px;
  text-transform: uppercase;
  letter-spacing: 0.04em;
  box-shadow: 0 4px 14px rgba(168, 85, 247, 0.4);
}

.plan-title {
  font-family: 'Sora', sans-serif;
  font-size: 1.8rem;
  font-weight: 800;
  color: #ffffff;
  text-align: center;
  margin-top: 8px;
  margin-bottom: 8px;
}

.plan-ideal {
  font-size: 0.9rem;
  color: var(--text-muted, #c4b8d7);
  line-height: 1.5;
  text-align: center;
  margin-bottom: 28px;
}

/* Estrutura de Preço */
.plan-price {
  background: rgba(168, 85, 247, 0.08);
  border: 1px solid rgba(168, 85, 247, 0.2);
  border-radius: 16px;
  padding: 18px 20px;
  text-align: center;
  margin-bottom: 28px;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 4px;
}

.price-main {
  color: #ffffff;
  display: flex;
  align-items: baseline;
  justify-content: center;
}

.price-currency {
  font-size: 1.2rem;
  font-weight: 700;
  margin-right: 2px;
}

.price-value {
  font-family: 'Sora', sans-serif;
  font-size: 3.2rem;
  font-weight: 800;
  line-height: 1;
}

.price-cents {
  font-size: 1.4rem;
  font-weight: 700;
}

.price-period {
  font-family: 'Sora', sans-serif;
  font-size: 0.95rem;
  font-weight: 700;
  color: #ffffff;
  display: block;
}

.price-renewal {
  font-size: 0.82rem;
  color: var(--text-muted, #c4b8d7);
  display: block;
}

/* Lista de Benefícios */
.plan-features {
  list-style: none;
  padding-top: 8px;
  margin: 0 0 32px 0;
}

.plan-features li {
  font-size: 0.95rem;
  color: var(--text-muted, #c4b8d7);
  margin-bottom: 14px;
  position: relative;
  padding-left: 28px;
  line-height: 1.5;
}

.plan-features li strong {
  color: #ffffff;
}

.plan-features li::before {
  content: "✓";
  position: absolute;
  left: 0;
  color: var(--highlight, #a855f7);
  font-weight: 800;
  font-size: 1.1rem;
}

.plan-features code {
  background-color: rgba(168, 85, 247, 0.2);
  color: #d8b4fe;
  padding: 2px 8px;
  border-radius: 6px;
  font-size: 0.88em;
  font-family: monospace;
}

/* Botão & Garantia */
.btn-featured {
  display: block;
  width: 100%;
  padding: 16px 20px;
  text-align: center;
  background: linear-gradient(135deg, var(--primary, #8343c3), var(--highlight, #a855f7));
  color: #ffffff;
  text-decoration: none;
  font-family: 'Sora', sans-serif;
  font-size: 1.05rem;
  font-weight: 700;
  border-radius: 12px;
  transition: all 0.2s ease;
  box-shadow: 0 6px 20px rgba(168, 85, 247, 0.35);
  box-sizing: border-box;
}

.btn-featured:hover {
  transform: translateY(-2px);
  box-shadow: 0 8px 25px rgba(168, 85, 247, 0.5);
  filter: brightness(1.08);
}

.guarantee-text {
  display: block;
  text-align: center;
  font-size: 0.8rem;
  color: var(--text-muted, #c4b8d7);
  margin-top: 12px;
}

/* Banner Upsell Catálogo */
.upsell-banner {
  background: rgba(26, 20, 34, 0.8);
  border: 1px dashed rgba(168, 85, 247, 0.3);
  border-radius: 16px;
  padding: 20px 24px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 20px;
}

.upsell-content {
  display: flex;
  align-items: center;
  gap: 16px;
}

.upsell-icon {
  font-size: 1.8rem;
}

.upsell-text h4 {
  font-family: 'Sora', sans-serif;
  color: #ffffff;
  font-size: 0.98rem;
  font-weight: 700;
  margin-bottom: 4px;
}

.upsell-text p {
  color: var(--text-muted, #c4b8d7);
  font-size: 0.86rem;
  margin: 0;
  line-height: 1.4;
}

.btn-upsell {
  white-space: nowrap;
  color: #ffffff;
  background: rgba(168, 85, 247, 0.15);
  border: 1px solid rgba(168, 85, 247, 0.4);
  padding: 10px 18px;
  border-radius: 8px;
  text-decoration: none;
  font-family: 'Sora', sans-serif;
  font-size: 0.85rem;
  font-weight: 600;
  transition: all 0.2s ease;
}

.btn-upsell:hover {
  background: rgba(168, 85, 247, 0.3);
  border-color: var(--highlight, #a855f7);
}

/* Responsividade */
@media (max-width: 768px) {
  .upsell-banner {
    flex-direction: column;
    text-align: center;
  }

  .upsell-content {
    flex-direction: column;
  }

  .btn-upsell {
    width: 100%;
    text-align: center;
  }
}

@media (max-width: 640px) {
  .plans-section {
    padding: 50px 16px;
  }

  .plan-card.single-card {
    padding: 32px 20px;
  }

  .price-value {
    font-size: 2.8rem;
  }
}
</style>